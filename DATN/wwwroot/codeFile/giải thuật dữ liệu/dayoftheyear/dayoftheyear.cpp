#include <bits/stdc++.h>
using namespace std;

int solution(int d, int m, int y);
int main(int argc, char** argv) {
    int d = atoi(argv[1]);
    int m = atoi(argv[2]);
    int y = atoi(argv[3]);
    cout << solution(d, m, y);
}